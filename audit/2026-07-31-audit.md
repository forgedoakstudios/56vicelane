# Nightly Site Audit — 2026-07-31

Supersedes the summary the Routine actually committed under a garbled
filename (`audit/## Nightly audit findings, 2026-07-31 — real code fixes,
not yet applied` on `main`, commit `9ad136a`) — the Routine wrote its
intended H2 heading text as the filename instead of the path it referenced
in its own body text (`audit/2026-07-31-audit.md`). That's a real bug in
the Routine worth fixing separately; this file is the report it should
have produced, with all four claimed findings independently re-verified
against the actual files rather than taken on trust.

## Findings and fixes applied

1. **Broken self-share links — real 404, fixed.**
   `articles/gta6-trailer-3-window-confirmed-56vicelane-called-it.html`
   self-referenced the wrong slug (`gta6-trailer-3-window-confirmed`,
   missing `-56vicelane-called-it`) in 8 places: `og:url`, `canonical`,
   and all 6 Reddit/Twitter/WhatsApp share button hrefs (two share bars,
   top and bottom of the article). No article at the short slug has ever
   existed. Fixed: all 8 corrected to the real slug.

2. **Canonical/og:url split 20-vs-53 — confirmed and standardized.**
   Verified directly: 20 of 73 articles used `/articles/slug.html` for
   both `canonical` and `og:url`; the other 53 (and `sitemap.xml` and
   `articles.json`, 100% of both, no exceptions) already used the
   extensionless form. Fixed: all 20 outliers' `canonical` and `og:url`
   tags stripped to the extensionless form, matching the other 53 and the
   sitemap. Share-button hrefs and other in-body links were left as-is —
   out of scope for this specific fix, not part of what was reported
   broken.

3. **12 articles with relative (non-absolute) og:image/twitter:image —
   confirmed and fixed.** All 12 used the identical relative path
   `/images/Jason_and_Lucia_01_landscape.jpg` for both tags. Fixed: all 24
   tags (12 files × og:image + twitter:image) made absolute
   (`https://56vicelane.com/images/Jason_and_Lucia_01_landscape.jpg`),
   matching the 9 other articles already using this same image correctly.

## Finding that did NOT check out

4. **Claimed: "og:title is literally 'Trevor'" on `trevors-take-ai-slop.html`
   and `trevors-take-gta6-expectations.html`.** Checked both files directly
   — both already have proper, distinct, descriptive `og:title` values
   ("Trevor's Take: The Fake Trailers Are Getting More Views Than My Life"
   and "Trevor's Take: Everybody Needs To Calm Down About GTA6"). No file
   in the repo has `og:title` set to the literal string "Trevor". This
   claim from the Routine's summary does not match the actual files —
   left untouched since there was nothing to fix.

## Not checked this run

The Routine's own summary noted its network egress returned 403 on every
external host, including 56vicelane.com itself, so it couldn't do live
HTTP checks (confirming actual redirect/404 behavior) or spot-check the 11
external citation links found in article bodies. This session didn't
re-attempt that either — worth a follow-up specifically for live-URL
verification if that's still wanted.

## Worth fixing separately (not done here, not a content fix)

The Routine's filename bug (see top of this file) — it should be writing
to `audit/YYYY-MM-DD-audit.md` and clearly isn't; two prior nights also
produced nothing at all per the 2026-07-30 daily checklist. Worth
reviewing the Routine's own prompt/logic rather than re-firing and hoping.
