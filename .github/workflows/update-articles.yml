name: Auto-Update articles.json

on:
  push:
    paths:
      - 'articles/**'
      - 'drafts/**'
  workflow_dispatch:

jobs:
  update-articles:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Rebuild articles.json
        run: node tools/build-articles.js

      - name: Commit updated articles.json
        run: |
          git config user.name "56ViceLane Bot"
          git config user.email "bot@56vicelane.com"
          git add articles.json
          git diff --staged --quiet || git commit -m "Auto-update articles.json [skip ci]"
          git push
